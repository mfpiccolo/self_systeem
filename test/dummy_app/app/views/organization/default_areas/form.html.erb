<% content_for :tabbar_title do %>
  <%= @organization.name %>
<% end %>

<div class="form-panel clearfix">
  <ul class="breadcrumbs">
    <li><%= link_to @organization.name, @organization %></li>
    <li><%= link_to "edit", edit_organization_path(@organization, @project, anchor: "areas") %></li>
    <li class="current"><a href="#"><%= @area.new_record? ? "New" : "Edit" %> Area</a></li>
  </ul>

  <%= simple_form_for [@organization, @area] do |f| %>
    <%= f.input :name, required: false %>
    <%= f.button :submit, class: "tiny button radius right" %>
  <% end %>
</div>
